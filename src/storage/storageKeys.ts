export const STORAGE_KEYS = {
  HAS_ONBOARDED: '@daily_upgrade/has_onboarded',
  USER_PROFILE: '@daily_upgrade/user_profile',
  TOPIC_PROGRESS: '@daily_upgrade/topic_progress',
  THEME_MODE: '@daily_upgrade/theme_mode',
} as const;
